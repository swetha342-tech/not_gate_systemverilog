module not_gate_tb;
  logic a;
  logic y;
  not_gate dut(
    .a(a), .y(y));
  initial begin
    $monitor("time=%t | a=%b | y=%b",$time,a,y);
    $dumpfile("waveform.vcd");
    $dumpvars(0,not_gate_tb);
    a=1; #10
    a=0; #10
    $finish;
  end
endmodule
    
